{
  "name": "minigame-test",
  "private": true,
  "scripts": {
    "check": "pnpm exec tsc --noEmit",
    "prebuild": "pnpm run check",
    "build": "pnpm exec vite build",
    "watch": "pnpm run build --watch"
  },
  "dependencies": {
    "@std/assert": "npm:@jsr/std__assert@^1.0.18",
    "minigame-api-typings": "^3.8.16",
    "minigame-std": "workspace:*"
  }
}
